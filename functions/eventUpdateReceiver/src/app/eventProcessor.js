exports.processEvent = async function(event){
    // update history
    
    if(event.status==='OK'){
        // delete ttl entity
        
        // putRecord on Kinesis
    } else {
        // if retry required
            // put ttl entity to reset timer
    }
}